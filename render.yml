services:
  - type: web
    name: sms-app
    env: python
    plan: free
    buildCommand: pip install -r app/requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
